# NewsMobile 界面对齐验收

## 本次运行结论

- 阶段：`04-harmony-visual-verify`
- 运行时间：2026-06-11
- 结论：部分完成，视觉截图采集阻塞。
- iOS 截图：已读取并覆盖 `output/01-ios-analyze/ios界面清单.md` 中 14 个页面。
- Harmony 构建：已通过。
- Harmony 截图：未生成，原因是本机缺少独立 Harmony command-line-tools 布局，`harmonyos-live-preview` 无法定位 `<command-line-tools>/bin/hvigorw`。
- 当前可用产物：`NewsMobileHarmony/entry/build/default/outputs/default/entry-default-unsigned.hap`

## 构建结果

使用 DevEco Studio hvigor CLI 在工作区临时 HOME 下构建：

```bash
export HOME=/Users/bb/work/appConvert/.hvigor-home
export HVIGOR_USER_HOME=/Users/bb/work/appConvert/.hvigor-home
export DEVECO_PATH=/Applications/DevEco-Studio.app
export DEVECO_SDK_HOME="$DEVECO_PATH/Contents/sdk"
export JAVA_HOME="$DEVECO_PATH/Contents/jbr/Contents/Home"
export PATH="/opt/homebrew/bin:$JAVA_HOME/bin:$PATH"
"$DEVECO_PATH/Contents/tools/node/bin/node" \
  "$DEVECO_PATH/Contents/tools/hvigor/bin/hvigorw.js" \
  --mode module \
  -p product=default \
  assembleHap \
  --analyze=normal \
  --parallel \
  --incremental
```

- 结果：`BUILD SUCCESSFUL`
- 产物：`NewsMobileHarmony/entry/build/default/outputs/default/entry-default-unsigned.hap`
- 签名：未配置 `signingConfigs`，构建跳过签名；未修改 `build-profile.json5` 或签名文件。
- 预览阻塞：`node /Users/bb/.codex/skills/harmonyos-live-preview/scripts/preview.mjs --project /Users/bb/work/appConvert/NewsMobileHarmony --page pages/Index --port 8088 --no-watch --keep-alive` 返回 `HarmonyOS command-line-tools not found. Set HARMONY_CLT or pass --clt <dir> (expected <dir>/bin/hvigorw).`

## Harmony 截图产物

- 目录：`output/04-harmony-visual-verify/screenshots/`
- 本次未生成可用截图。
- 阻塞原因：当前环境只有 DevEco Studio 内置 hvigor 路径 `/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw.js`，但 live preview 工具要求独立 command-line-tools 风格路径 `<dir>/bin/hvigorw` 和 SDK Previewer 配套布局。

## UI/UX 修复说明

本次在 `03-harmony-project-generate` 阶段已完成以下 UI 修复，并通过构建：

- 删除全局 `NewsMobile / HarmonyOS NEXT migration build` 顶栏，改为每个页面自己的大标题。
- 首页改为 `News` 大标题、右上 `Refresh`、趋势 topic、分类 chip、文章卡片。
- For You 改为 `For You` 大标题和右上 `More`。
- Search 增加 `Search` 大标题，空态文案改为搜索新闻语义。
- Saved 增加 `Saved` 大标题、`Edit` 入口、空态和紧凑列表行，并保留未读蓝点。
- Article Detail 改为 `Article / Done` 顶部、正文信息层级、`Read Full Article` 主按钮、`Save / Listen / Share` 三列操作。
- 数据层改为真实 RSS/Atom 优先，fixture 只做网络或解析失败兜底。
- fixture 中 `example.com` 占位 URL 已替换为真实新闻或订阅域名。

## 逐页对齐记录

| 页面 | iOS 截图 | Harmony 落点 | 当前判断 | 未完成项 |
| --- | --- | --- | --- | --- |
| 首页新闻流 | `01-home.png` | `Index.homePage()` | 已按截图修正大标题、刷新、趋势、分类、卡片和真实数据状态 | 需生成 Harmony 截图做像素级/视觉级复核 |
| 分类新闻 | `02-home-category.png` | `selectedCategory` + `filteredArticles()` | 已保留分类筛选和卡片样式 | 需确认是否要独立分类页或继续同页筛选 |
| 文章详情 | `03-article-detail.png` | `articleDetail()` | 已修正 Article/Done、主按钮和三列操作区 | 需截图确认标题换行、按钮间距、底部内容 |
| 文章 WebView | `04-article-webview.png` | `Open Original` 接入点 | 已保留 WebView 语义入口 | 仍需真实 ArkUI Web 页面和截图 |
| For You | `05-for-you.png` | `forYouPage()` | 已修正大标题和更多入口 | 需截图确认推荐流卡片密度 |
| 搜索空态 | `06-search-empty.png` | `searchPage()` | 已实现搜索框和空态 | 需截图确认空态是否足够居中、图标是否需要补齐 |
| 搜索结果 | `07-search-results.png` | `searchPage()` + `filteredArticles()` | 已实现结果过滤 | 当前仍复用 articleCard，可能比 iOS 结果行更重 |
| 收藏空态 | `08-saved-empty.png` | `savedPage()` | 已实现空态 | 需截图确认居中位置 |
| 收藏有数据 | `09-saved-with-article.png` | `savedRow()` | 已实现 Edit、列表行、未读蓝点 | 需截图确认行高和分隔线 |
| 设置页 | `10-settings.png` | `settingsPage()` | 已保留设置项入口 | 当前为普通行列表，分组 Form 视觉仍需细化 |
| 关键词提醒 | `11-keyword-alerts.png` | `KeywordAlertService` + 设置入口 | 保留功能入口 | 子页 UI 尚未完整复刻 |
| 自定义订阅源 | `12-custom-feeds.png` | `NewsService.sources` + 设置入口 | 保留源列表和入口 | 子页 UI 尚未完整复刻 |
| 本地新闻 | `13-local-news.png` | 设置入口 + Local 分类语义 | 保留入口 | 子页 UI 尚未完整复刻 |
| 语音播报 | `14-audio-briefing.png` | `TtsService` + Article Listen 入口 | 保留播放入口 | 独立 Audio Briefing 页面尚未完整复刻 |

## 下一步

1. 安装或配置独立 Harmony command-line-tools，并设置 `HARMONY_CLT` 或传入 `--clt <dir>`。
2. 重新运行 `04-harmony-visual-verify`，生成 `output/04-harmony-visual-verify/screenshots/` 下的 Harmony 截图。
3. 用 Harmony 截图逐页更新本文件，把当前“需截图确认”的项改为已通过或修复项。
4. 对差异明显的页面继续修 `Index.ets`，重点是底部 Tab 图标、搜索结果行、设置分组表单、WebView 子页、关键词/订阅源/本地新闻/音频播报子页。
